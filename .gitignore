# Dependencies
node_modules/
**/node_modules/

# Build output
dist/
build/
**/dist/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment / local settings (this app uses managed identity — no secrets in repo)
.env
.env.*
!.env.example
local.settings.json

# Azure / tooling
.azure/
*.pfx
*.pem
*.key

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Playwright / test artifacts
.playwright-mcp/
playwright-report/
test-results/

# Session / scratch
*.tmp
app/scripts/news-agent.env
app/scripts/deal-agent.env
app/scripts/persona-agents.env
app/scripts/_sample-news.json
app/scripts/__pycache__/
deal-room-teams.zip

# Local planning / chat context (never commit — fork is public, may contain tenant/sub IDs)
plan.md
plan.codespace.md
*-chat-archive/
deal-room-chat-archive/

_cm.txt

# Local npm registry override (Intune-managed feed 404s; use public registry locally)
.npmrc

# Generated by scripts/provision-entra.* (contains app IDs; regenerate per env)
entra.generated.bicepparam

# Internal delivery / ops runbooks (tenant-specific — NOT part of the shipped
# accelerator; the README is the customer-facing documentation).
docs/SOLUTION.md
docs/one-pager.md
docs/transformation-plan.md
docs/dealhub-architecture.drawio

# Compiled Bicep output (generated by `az bicep build` / `build-params`).
# The hand-authored azd param map (infra/main.parameters.json) is kept.
infra/main.json
infra/main.dev.json
infra/main.test.json
infra/main.prod.json
infra/main.sample.json
